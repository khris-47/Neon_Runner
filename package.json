{
  "name": "neon-runner",
  "version": "0.1.0",
  "description": "Neon Runner, juego procedural para navegador con estetica neon futurista.",
  "type": "module",
    "allowScripts": {
    "esbuild": true
  },
"scripts": {
  "dev": "vite",
  "build": "npx vite build",
  "preview": "vite preview"
},
  "dependencies": {
    "three": "^0.160.0"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}